console.log(Math.random()); //0과 1사이 값을 반환
console.log(Math.floor(0.12342)); //소수점 없애줌
console.log(Math.ceil(1.23123234)); //소수값이 존재할때 값을 올림
console.log(Math.round(0.4));
//소수점 반올림
console.log(Math.max(1, 2, 3, 4)); //여러개의 값들 중 가장 큰 값 반환
console.log(Math.min(1, 2, 3, 4));
//여러개의 값들 중 가장 작은 값 반환
